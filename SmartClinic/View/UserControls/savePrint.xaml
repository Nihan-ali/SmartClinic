<UserControl x:Class="SmartClinic.View.UserControls.savePrint"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartClinic.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="50*"/>
        </Grid.ColumnDefinitions>
        <Grid>
            <Button Content="Save" Background="#FF28BF6F" FontSize="13" FontWeight="Bold" Opacity="0.895" Foreground="#FFF5EAEA" ScrollViewer.CanContentScroll="True" OpacityMask="#FF257A8A"/>
        </Grid>
        <Grid Grid.Column="1">
            <Rectangle Fill="LightGreen"/>
            <Button Content="Save &amp; Print" FontSize="13" Background="#FF28BF6F" FontWeight="Bold" Opacity="0.895" Foreground="#FFF5EAEA"/>
        </Grid>

    </Grid>
</UserControl>
