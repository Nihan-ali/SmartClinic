<UserControl x:Class="SmartClinic.View.UserControls.savePrint"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartClinic.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="50*"/>
        </Grid.ColumnDefinitions>
        <Grid>
            <Button Content="Save" Style="{StaticResource SaveButtonStyle}"/>
        </Grid>
        <Grid Grid.Column="1">
            <Rectangle Fill="LightGreen"/>
            <Button Content="Save &amp; Print" Style="{StaticResource SaveButtonStyle}" />
        </Grid>

    </Grid>
</UserControl>
