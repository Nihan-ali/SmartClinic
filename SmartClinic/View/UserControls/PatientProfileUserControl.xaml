<UserControl x:Class="SmartClinic.View.UserControls.PatientProfileUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">

    <Grid Background="White">
        <!-- Add your UI elements here to display patient profile information -->
        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="18" Margin="10"/>
        <TextBlock Text="{Binding Phone}" Margin="10"/>
        <TextBlock Text="{Binding Address}" Margin="10"/>
        <!-- Add more as needed -->
    </Grid>
</UserControl>
