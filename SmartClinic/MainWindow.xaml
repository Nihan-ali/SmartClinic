<Window x:Class="SmartClinic.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmartClinic"
        mc:Ignorable="d"
        Title="Doctor Rx"
        WindowState="Maximized"
        Icon="doctor.ico" 
        Height="{x:Static SystemParameters.PrimaryScreenHeight}" 
        Width="{x:Static SystemParameters.PrimaryScreenWidth}"
        >
    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Visible">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="6*"/>
                <RowDefinition Height="94*"/>
            </Grid.RowDefinitions>
            <Grid>
                <Rectangle Fill="#FF332646"/>
                <Label Content="Digital Rx" HorizontalAlignment="Left" VerticalAlignment="Center"  Foreground="#FFF5E8E8" FontSize="16" FontWeight="Bold"/>
                <Label Content="Dr. Abu Noyem Mohammad" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#FFF1E1E1" FontSize="14" FontWeight="Bold"/>
            </Grid>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="4*"/>
                    <ColumnDefinition Width="96*"/>
                </Grid.ColumnDefinitions>
                <Grid>
                    
                    <Rectangle Fill="#FF332646"/>
                    <StackPanel>
                        <Button Name="rx" Margin="15,15" Background="{x:Null}" BorderBrush="{x:Null}">
                            <Image Source="rx.png" Height="20" Width="20"/>
                        </Button>
                        <Button Name="patients" Margin="15,15" Background="{x:Null}" BorderBrush="{x:Null}">
                            <Image Source="patient.png" Height="20" Width="20"/>
                        </Button>
                        <Button Name="settings" Margin="15,15" Background="{x:Null}" BorderBrush="{x:Null}">
                            <Image Source="settings.png" Height="20" Width="20"/>
                        </Button>
                        <Button Name="stat" Margin="15,15" Background="{x:Null}" BorderBrush="{x:Null}">
                            <Image Source="stat.png" Height="20" Width="20"/>
                        </Button>
                    </StackPanel>
                </Grid>
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition x:Name="searching" Height="4*"/>
                        <RowDefinition Height="96*"/>
                    </Grid.RowDefinitions>
                    <Grid>
                        <Rectangle Fill="AliceBlue"/>
                        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                            <Button x:Name="prescription1" Content="New Prescription" Margin="0,0,10,0" BorderBrush="{x:Null}" Foreground="#FF1A1840" FontWeight="Bold" Background="{x:Null}"/>
                            <!-- Add margin to create space between buttons -->
                            <Button Content="Print Preview" Margin="0,0,10,0" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="#FF5556C1" FontSize="10" FontWeight="Bold"/>
                            <TextBox Text="search here" Height="25" Background="#FFCADAE4" Width="96" Opacity="0.5" SelectionOpacity="-6" SelectionBrush="#FF7E94A5"/>
                        </StackPanel>
                    </Grid>
                    <Grid Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition x:Name="name" Height="7*"/>
                            <RowDefinition x:Name="patient" Height="4*"/>
                            <RowDefinition Height="80*"/>
                            <RowDefinition Height="4*"/>
                            <RowDefinition Height="5*"/>
                        </Grid.RowDefinitions>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50*"/>
                                <ColumnDefinition Width="50*"/>
                            </Grid.ColumnDefinitions>
                            <Grid>
                                <StackPanel>
                                    <Label Content="DR. ABU NOYEM MOHAMMAD" FontWeight="DemiBold" Foreground="#FF4FB14F" FontSize="25" Margin="0,0,0,-10" />
                                    <Label Content="MBBS, (Endocrinology &amp; Metabolism)" Margin="0,-5,0,0" FontStyle="Italic" FontWeight="DemiBold" />
                                </StackPanel>
                            </Grid>
                            <Grid Grid.Column="1">
                                <StackPanel HorizontalAlignment="Right">
                                    <Label Content="ডা.আবু নঈম মোহাম্মাদ" FontWeight="DemiBold" Foreground="#FF4FB14F" FontSize="25" Margin="0,0,0,-8" />
                                    <Label Content="এমবিবিএস, (এন্ডোক্রাইনোলজি &amp; মেটাবোলিজম)" HorizontalAlignment="Right" Margin="0,-5,0,0" FontWeight="Bold" />
                                </StackPanel>
                            </Grid>
                            
                        </Grid>
                        
                        <Grid Grid.Row="1">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Name Label and Add Patient Button -->
                            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="10">
                                <Label Content="Name:" VerticalAlignment="Center"/>
                                <Button Content="+ Add Patient" Foreground="Blue" FontWeight="Bold" Background="{x:Null}" BorderBrush="{x:Null}" VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- Today's Date -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <Label Content="Date: " VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{Binding TodayDate}" VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- Patient's Age -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                <Label Content="Age: " VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="age here" VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- Search Patient TextBox and Button -->
                            <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Button Name="search" Background="{x:Null}" BorderBrush="{x:Null}">
                                    <Image Source="search.png" Height="15" Width="15"/>
                                </Button>
                                <TextBox x:Name="searchPatientTextBox" Text="Search Patient" Width="200" Margin="0,0,10,0" Foreground="Gray"  GotFocus="OnSearchTextBoxGotFocus" LostFocus="OnSearchTextBoxLostFocus"/>
                                
                            </StackPanel>
                        </Grid>
                        <Border Grid.Row="1" BorderBrush="Black" BorderThickness="0,1,0,0"/>
                        <Grid x:Name="prescription" Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="20*"/>
                                <ColumnDefinition Width="80*"/>
                            </Grid.ColumnDefinitions>
                            <Grid>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Chief Complaint" FontWeight="Bold" FontSize="14" Margin="10,25,0,0"/>
                                        <Button Content="+" Margin="0,25,0,0" Background="{x:Null}" FontSize="16" FontWeight="Bold" BorderBrush="{x:Null}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="History" FontWeight="Bold" FontSize="14" Margin="10,25,0,0"/>
                                        <Button Content="+" Margin="0,25,0,0" Background="{x:Null}" FontSize="16" FontWeight="Bold" BorderBrush="{x:Null}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="On Examinations" FontWeight="Bold" FontSize="14" Margin="10,25,0,0"/>
                                        <Button Content="+" Margin="0,25,0,0" Background="{x:Null}" FontSize="16" FontWeight="Bold" BorderBrush="{x:Null}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Investigation" FontWeight="Bold" FontSize="14" Margin="10,25,0,0"/>
                                        <Button Content="+" Margin="0,25,0,0" Background="{x:Null}" FontSize="16" FontWeight="Bold" BorderBrush="{x:Null}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Diagonosis" FontWeight="Bold" FontSize="14" Margin="10,25,0,0"/>
                                        <Button Content="+" Margin="0,25,0,0" Background="{x:Null}" FontSize="16" FontWeight="Bold" BorderBrush="{x:Null}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="Treatment Plan" FontWeight="Bold" FontSize="14" Margin="10,25,0,0"/>
                                        <Button Content="+" Margin="0,25,0,0" Background="{x:Null}" FontSize="16" FontWeight="Bold" BorderBrush="{x:Null}"/>
                                    </StackPanel>

                                </StackPanel>
                            </Grid>
                            <Grid Grid.Column="1">
                                <Label Content="Hello Doctors" FontSize="45" FontWeight="Bold" Foreground="Blue" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                            <Border Grid.Column="1" BorderBrush="Black" BorderThickness="1,0,0,0"/>
                        </Grid>
                        <Border Grid.Row="2" BorderBrush="Black" BorderThickness="0,1,0,0"/>
                        <Grid x:Name="save" Grid.Row="3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50*"/>
                                <ColumnDefinition Width="50*"/>
                            </Grid.ColumnDefinitions>
                            <Grid>
                                <Button Content="Save" Background="#FF28BF6F" FontSize="13" FontWeight="Bold" Opacity="0.895" Foreground="#FFF5EAEA" ScrollViewer.CanContentScroll="True" OpacityMask="#FF257A8A"/>
                            </Grid>
                            <Grid Grid.Column="1">
                                <Rectangle Fill="LightGreen"/>
                                <Button Content="Save &amp; Print" FontSize="13" Background="#FF28BF6F" FontWeight="Bold" Opacity="0.895" Foreground="#FFF5EAEA"/>
                            </Grid>
                        </Grid>
                        <Grid x:Name="fotter" Grid.Row="4">
                            <StackPanel>
                                <TextBlock FontWeight="Medium" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Run Text="Registered to"/>
                                    <Run FontWeight="Bold" Text="Dr. Abu Noyem Mohammad +8801619800333"/>
                                </TextBlock>
                                <TextBlock FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Run Text=" &#169;"/>
                                    <Run Text="All rights reserved to SmartClinic"/>
                                </TextBlock>
                            </StackPanel>
                            
                        </Grid>
                    </Grid>


                </Grid>


            </Grid>
        </Grid>



    </ScrollViewer>
</Window>
    
